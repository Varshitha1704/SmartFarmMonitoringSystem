# Optional runtime configuration. Environment variables can also be used with SMARTFARM_ prefix.
db.url=jdbc:mysql://localhost:3306/smartfarm
db.user=root
db.password=

openweather.key=
weather.city=Delhi

twilio.sid=
twilio.token=
twilio.from=
twilio.to=

# Optional. Examples: python, python3, py -3.
# Use forward slashes for Windows paths because Java .properties treats backslash as an escape.
python.command=py -3.11
